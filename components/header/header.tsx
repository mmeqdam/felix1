"use client";

import Navigation from "./navigation";

const Header = () => {
  return (
    <header className="w-full sticky top-0 z-50">
      <Navigation />
    </header>
  );
};

export default Header;
